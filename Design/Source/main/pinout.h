#include "pins.h"

#define DMX_TX_PIN 45  // the pin we are using to TX with
#define DMX_RX_PIN 46  // the pin we are using to RX with
#define DMX_EN_PIN 47  // the pin we are using to enable TX on the DMX transceiver