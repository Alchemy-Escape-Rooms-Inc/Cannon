#include "pins.h"

#define DMX_TX_PIN 17  // the pin we are using to TX with
#define DMX_RX_PIN 16  // the pin we are using to RX with
#define DMX_EN_PIN 21  // the pin we are using to enable TX on the DMX transceiver